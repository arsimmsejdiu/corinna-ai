import React from "react";
import { FieldError, FieldValue, UseFormRegister } from "react-hook-form";
import { PortalStepsProps } from "@/constants/types";

const PortalSteps = ({
    questions,
    type,
    register,
    error,
    onNext,
    step,
    onBooking,
    date,
    onBack,
    onSlot,
    loading,
    slot,
    products,
    bookings,
    amount,
    stripeId,
  }: PortalStepsProps) => {
    
  }
